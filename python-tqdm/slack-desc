# HOW TO EDIT THIS FILE:
# The "handy ruler" below makes it easier to edit a package description.
# Line up the first '|' above the ':' following the base package name, and
# the '|' on the right side marks the last column you can put a character in.
# You must make exactly 11 lines for the formatting to be correct.  It's also
# customary to leave one space after the ':' except on otherwise blank lines.

    |-----handy-ruler------------------------------------------------------|
python-tqdm: python-tqdm (progress bar for Python)
python-tqdm:
python-tqdm: A fast, extensible progress bar for Python. Instantly make your
python-tqdm: loops show a smart progress meter - just wrap any iterable with
python-tqdm: python-tqdm(iterable), and you're done!
python-tqdm:
python-tqdm: Homepage: https://github.com/python-tqdm/python-tqdm
python-tqdm:
python-tqdm:
python-tqdm:
python-tqdm:
