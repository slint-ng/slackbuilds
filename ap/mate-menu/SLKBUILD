# shellcheck shell=bash
# Maintainer: Frederic Bezies <fredbezies at gmail dot com>
# Contributor: Balló György <ballogyor+arch at gmail dot com>

#Included in Slint by Didier Spaier didieratslintdotfr
pkgname=mintmenu
export commit=a09d499
pkgver=git$commit
pkgrel=2slint
slackdesc="$pkgname (Advanced menu for MATE Panel)"
docs=("copying" "authors" "README.html")
options=("nosrcpack")
url="https://github.com/linuxmint/mintmenu"
license=('GPL')
docs=("COPYING")
depends=('mate-panel' 'python-configobj' 'python3-gobject' 'python3-pyinotify' 'python-xdg' 'python3-xlib' 'xdg-utils' 'python3-setproctitle' 'mate-menus' 'python3-cairo')
makedepends=('python3-distutils-extra' 'python3-setuptools')
#source=("https://github.com/ubuntu-mate/mate-menu/archive/$pkgver.tar.gz")
sha256sums=('36aa865b72664b8cc90d9c1c429fd3c44e2c198369aae4eaaa87228a5add075c')

build() {
  git clone https://github.com/linuxmint/mintmenu
  cd $pkgname
  git checkout $commit
  cp -r usr $PKG
}
