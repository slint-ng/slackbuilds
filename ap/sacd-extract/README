This package includes a single command:
sacd_extract
that extract DSD files from an SACD image
Mind the spelling of the command:
it includes an underscore '_' not an hyphen '-'.

By default ISO image is supposed to come from  a CDROM (an SACD) but it
can also be retrieved fom a file stored locally or remotely.

To knw more type:
sacd_extract --help

The resulting DSD files can then be read by mpd-sacd.
