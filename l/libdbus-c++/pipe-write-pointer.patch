--- a/src/pipe.cpp
+++ b/src/pipe.cpp
@@ -83,5 +83,6 @@
 void Pipe::signal()
 {
   // TODO: ignoring return of read/write generates warning; maybe relevant for eventloop work...
-  ::write(_fd_write, '\0', 1);
+  static const char nullByte = '\0';
+  ::write(_fd_write, &nullByte, 1);
 }
