#Packager: Didier Spaier didierspaieratslintdotfr

pkgname="slint"
pkgver=1
pkgrel=1slint
arch=noarch

slackdesc=\
(
#|-----handy-ruler------------------------------------------------------|
"slint (default set of packages)"
"Typing as root \"slapt-get --install-set slint\" --reinstall will"
"install or update all packages included in Slint by default."
)

build() {
	cd $startdir/src
} 
