interface:
  display_name: "SLKBUILD Validation"
  short_description: "Validate SLKBUILD build logs and package artifacts"
  default_prompt: "Validate build results from ~/slkbuilds/<category>/<pkg> and return a checklist with Artifacts, Log Health, Payload Checks, Verdict, and Next Action."

